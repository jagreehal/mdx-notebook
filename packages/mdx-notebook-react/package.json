{
  "name": "mdx-notebook-react",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" },
    "./package.json": "./package.json"
  },
  "files": ["dist"],
  "scripts": {
    "build": "bash -lc 'set -o pipefail; FILTER=\"Invalid input options \\\\(1 issue found\\\\)|For the \\\\\\\"define\\\\\\\"\\\\. +Invalid key|\\\\[PLUGIN_TIMINGS\\\\]|Your build spent significant time in plugin|See https://rolldown.rs/options/checks#plugintimings|rolldown-plugin-dts:generate|rolldown-plugin-dts:resolver\"; tsdown 2>&1 | { if command -v rg >/dev/null 2>&1; then rg -v \"$FILTER\" || true; else grep -Ev \"$FILTER\" || true; fi; }; exit ${PIPESTATUS[0]}'",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo coverage"
  },
  "dependencies": {
    "katex": "^0.16.0",
    "mdx-notebook-core": "workspace:*"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0",
    "recharts": "^2.0.0 || ^3.0.0",
    "mermaid": "^11.0.0"
  },
  "peerDependenciesMeta": {
    "recharts": { "optional": true },
    "mermaid": { "optional": true }
  },
  "devDependencies": {
    "@types/katex": "^0.16.0",
    "@types/react": "^19.0.0",
    "@testing-library/react": "^16.0.0",
    "jsdom": "^26.0.0",
    "react": "^19.0.0"
  }
}
