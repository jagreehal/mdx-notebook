{
  "name": "create-mdx-notebook",
  "version": "0.0.0",
  "description": "Scaffold a new mdx-notebook tutorial site",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "create-mdx-notebook": "./dist/cli.js"
  },
  "files": ["dist", "template"],
  "scripts": {
    "build": "bash -lc 'set -o pipefail; FILTER=\"Invalid input options \\\\(1 issue found\\\\)|For the \\\\\\\"define\\\\\\\"\\\\. +Invalid key|\\\\[PLUGIN_TIMINGS\\\\]|Your build spent significant time in plugin|See https://rolldown.rs/options/checks#plugintimings|rolldown-plugin-dts:generate|rolldown-plugin-dts:resolver\"; tsdown 2>&1 | { if command -v rg >/dev/null 2>&1; then rg -v \"$FILTER\" || true; else grep -Ev \"$FILTER\" || true; fi; }; status=${PIPESTATUS[0]}; [ $status -eq 0 ] || exit $status; chmod +x dist/cli.js'",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo coverage template",
    "prebuild": "node ./scripts/sync-template.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
