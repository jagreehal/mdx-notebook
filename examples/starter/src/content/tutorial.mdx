# Welcome to mdx-notebook

This is a tutorial built with **mdx-notebook**. Code cells run at build time and their outputs render below.

## A simple inline cell

```ts run id=hello
console.log("Hello from inline TypeScript");
console.log("Multiple stdout lines are captured");

export default async function () {
  return { greeting: "hi", n: 42 };
}
```

## A file reference cell (TripMate agent)

The cell below points at a real `.ts` file. Build runs it, captures stdout and the default-export return.

:::run{src="../../scripts/agent.ts" id="trace"}
:::

## A Jupyter notebook import

Saved Python output rendered statically.

:::ipynb{src="../../notebooks/intro.ipynb" id="nb" cells="0"}
:::
