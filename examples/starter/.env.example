# mdx-notebook starter — environment variables
#
# Optional: provide an Anthropic API key to run AI tutorials against a real model.
# Without this, tutorials gracefully fall back to mocked output.
# Get a key at https://console.anthropic.com/settings/keys
#
# ANTHROPIC_API_KEY=sk-ant-...
